import { getCurrentPlaylist } from "./vars/get-current-playlist";
import { listPlaylists } from "./vars/list-playlists";
import { setCurrentPlaylist } from "./vars/set-current-playlist";

export const core = {
  getCurrentPlaylist: getCurrentPlaylist,
  listPlaylists: listPlaylists,
  setCurrentPlaylist: setCurrentPlaylist,
};
